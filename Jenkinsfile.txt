pipeline {
agent any
 stages{
    stage('Test on Windows') {

            steps {
                bat script: '''
                    pip install -r requirements.txt;
                    py.test tests/*.py'''

            }
            post {
                always {
                    junit '**/target/*.xml'
                }
}

}}}
