pipeline {
    agent  any
    stages {
        stage('Test') {
            steps {
                withpytest(pytest:'pytest-5.4.3')
                sh 'py.test -v'
            }
        }
    }
    post {
        always {
            junit 'reports/*.xml'
        }


}}
