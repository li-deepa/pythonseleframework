pipeline {

 stages{
    stage('Test on Windows') {
            agent {
                label 'windows'
            }
            steps {
                bat label: '', script: '''cd tests
                    pip install -r requirements.txt
                    py.test test_e2e.py
                    '''
            }
            post {
                always {
                    junit '**/target/*.xml'
                }
}

}}}
