pipeline {
    agent  any
    {

    node('MYNODE')

{ bat ''echo ON ''';
  bat label: '', script: '''PYENV_HOME=$WORKSPACE/tests/.pyenv/
}
    }
    stages {
        stage('Test') {
            steps {

                virtualenv --no-site-packages $PYENV_HOME
                source $PYENV_HOME/bin/activate
                pip install -U pytest
                pip install -r requirements.txt
                deactivate'''

            }
        }
    }
    post {
        always {
            sh'py.test --junit='reports/*.xml''
        }


}}
