pipeline {
    agent  any
    stages {
        stage('Test') {
            steps {
                 sh 'py.test -v'
            }
        }
    }
    post {
        always {
            junit 'reports/*.xml'
        }


}}
