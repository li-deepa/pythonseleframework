pipeline {
    agent  {
    node {

        customWorkspace '$WORKSPACE/tests/*.py/'
    }
  }

    stages {
        stage('Test') {
            steps {
                sh'pip install -U pytest'
                 sh'pip install -r requirements.txt'
                sh 'py.test -v'
            }
        }
    }

}}
