pipeline {
    agent  any
    stages {
        stage('Test') {
            steps {
                
                sh "pip install -r requirements.txt"
                sh "python -m compileall ."
                sh 'py.test'

            }
        }
    }
    post {
        always {
            sh'py.test --junit='reports/*.xml''
        }


}}
