pipeline {
    agent  any
    {

    node('MYNODE')

{ bat ''echo ON '''
}
    }
    stages {
        stage('Test') {
            steps {
                sh'bat label: '', script: '''PYENV_HOME=$WORKSPACE/tests/.pyenv/'
                sh'virtualenv --no-site-packages $PYENV_HOME'
                sh'source $PYENV_HOME/bin/activate'
                sh'pip install -U pytest'
                sh'pip install -r requirements.txt'


            }
        }
    }
    post {
        always {
            sh'py.test --junit='reports/*.xml''
        }


}}
